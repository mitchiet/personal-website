const LoadingSpinner = () => {

  return (
    <div className="flex justify-center items-center intro-swiper">
      <div className="animate-spin rounded-full h-10 w-10 border-t-2 border-b-2 spinner-blue"></div>
    </div>
  );
};

export default LoadingSpinner;
